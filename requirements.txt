# path: requirements.txt

# API
fastapi==0.111.0
uvicorn[standard]==0.30.0
httpx==0.27.0

# Database
sqlalchemy==2.0.30
alembic==1.13.1
psycopg2-binary==2.9.9
pgvector==0.3.2

# Settings & validation
pydantic==2.7.1
pydantic-settings==2.3.0
python-dotenv==1.0.1

# Task queue
celery==5.4.0
redis==5.0.4
flower==2.0.1

# AWS
boto3==1.34.0

# Testing
pytest==8.2.0
pytest-asyncio==0.23.7
httpx==0.27.0



# ── Phase 2 additions ────────────────────────────────────

# Data fetching
yfinance>=1.4.1   # stock price data — no API key needed
newsapi-python==0.2.7      # NewsAPI client
feedparser==6.0.11         # RSS feed parser
praw==7.7.1                # Reddit API

# Kafka
confluent-kafka==2.3.0     # Kafka producer and consumer

# ClickHouse
clickhouse-driver==0.2.7   # ClickHouse Python client

# Celery task queue
celery==5.4.0              # already in requirements — verify it's there
redis==5.0.4               # already in requirements — verify it's there
flower==2.0.1              # Celery monitoring UI

# Data processing
pandas==2.2.2              # data manipulation
numpy==1.26.4              # numerical operations

# ── ML Models ────────────────────────────────────────────
prophet==1.1.5
xgboost==2.0.3
statsmodels==0.14.2
scikit-learn==1.4.2

# ── NLP ──────────────────────────────────────────────────
spacy==3.7.5
sentence-transformers==2.7.0

# ── ML Tracking ──────────────────────────────────────────
mlflow==2.13.0

# ── Additional ───────────────────────────────────────────
joblib==1.4.2
scipy==1.13.0
typer==0.12.5